"""Local BLE pairing configuration.

The repository default is intentionally public and unsafe. Change it before
using this firmware with any real remote-controlled device.
"""

BLE_PAIRING_SECRET = "dangerous"
